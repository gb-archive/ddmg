image: dlang2/ldc-ubuntu

stages:
- install_dependencies
- build
- test

install-deps:
       stage: install_dependencies
       script: apt-get update -qq && apt-get install -y -qq libsdl2-dev libglfw3-dev

build-release:
       stage: build
       script: ci/build-release.sh
       artifacts:
               paths:
               - ddmg

build-trial:
        stage: build
        script: ci/build-trial.sh
        artifacts:
                paths:
                - trial/trial

unittests:
        script: ci/unittests.sh
        dependencies:
        - build-trial
